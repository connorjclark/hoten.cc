---
layout: default
permalink: "/blog/{{ title | slug }}/"
tags: ['post']
---
<article>
  <h1>{{ title }}</h1> 
  <time datetime="{{ date | dateIso }}">{{ date | dateReadable }}</time> 
  {{ content }}
</article>
